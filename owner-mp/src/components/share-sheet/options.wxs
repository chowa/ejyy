var PRESET_ICONS = ['qq', 'weibo', 'wechat', 'link', 'qrcode', 'poster'];

function getIconURL(icon) {
    if (PRESET_ICONS.indexOf(icon) !== -1) {
        return 'https://img.yzcdn.cn/cwt/share-icon-' + icon + '.png';
    }

    return icon;
}

module.exports = {
    getIconURL: getIconURL
};
